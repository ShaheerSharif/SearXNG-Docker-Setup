name: searxng

services:
  core:
    container_name: searxng-core
    image: docker.io/searxng/searxng:${SEARXNG_VERSION:-latest}
    restart: always
    env_file: ./.env
    volumes:
      - ./core-config/:/etc/searxng/:Z
      - searxng-core-data:/var/cache/searxng/
    networks:
      - searxng-network

  valkey:
    container_name: searxng-valkey
    image: docker.io/valkey/valkey:9-alpine
    command: valkey-server --save 30 1 --loglevel warning
    restart: always
    volumes:
      - searxng-valkey-data:/data/
    networks:
      - searxng-network

  nginx:
    container_name: searxng-nginx
    image: nginx:mainline-alpine
    restart: unless-stopped
    ports:
      - "80:80"
    env_file: ./.env
    environment:
      - DOMAIN_NAME=${SUBDOMAIN}.${DOMAIN}
      - BACKEND_URL=http://core:${SEARXNG_PORT:-8080}/
    command: >
      /bin/sh -c "envsubst '$${DOMAIN_NAME} $${BACKEND_URL}'
      < /etc/nginx/conf.d/default.conf.template
      > /etc/nginx/conf.d/default.conf
      && nginx -g 'daemon off;'"
    volumes:
      - ./nginx/default.conf.template:/etc/nginx/conf.d/default.conf.template:ro
    networks:
      - searxng-network
    depends_on:
      - core
      - valkey

volumes:
  searxng-core-data:
  searxng-valkey-data:

networks:
  searxng-network:
    driver: bridge
